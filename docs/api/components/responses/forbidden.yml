description: Forbidden
content:
  application/json:
    schema:
      $ref: ../schemas/error-response.yml
    examples:
      example:
        value:
          status: error
          error:
            code: FORBIDDEN
            message: Insufficient permissions

