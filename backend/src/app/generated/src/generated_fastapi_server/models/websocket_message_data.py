# coding: utf-8

"""
    Simple Note Application API

    Public-first API for browsing latest public notes and managing a personal notebook of plain text notes (for anonymous and regular users). Authentication is via Firebase ID tokens passed as `Authorization: Bearer <token>`.  ## Real-time Features This API includes WebSocket integration for real-time comment notifications. When comments are posted, they are automatically broadcasted to all connected WebSocket clients for live updates. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
from inspect import getfullargspec
import json
import pprint
import re  # noqa: F401



from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from generated_fastapi_server.models.websocket_comment_created import WebsocketCommentCreated
from generated_fastapi_server.models.websocket_comments_list import WebsocketCommentsList
from generated_fastapi_server.models.websocket_test_message import WebsocketTestMessage
from typing import Union, Any, List, TYPE_CHECKING, Optional, Dict
from typing_extensions import Literal
from pydantic import StrictStr, Field
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

WEBSOCKETMESSAGEDATA_ONE_OF_SCHEMAS = ["WebsocketCommentCreated", "WebsocketCommentsList", "WebsocketTestMessage"]

class WebsocketMessageData(BaseModel):
    """
    Message payload that varies based on type
    """
    # data type: WebsocketCommentCreated
    oneof_schema_1_validator: Optional[WebsocketCommentCreated] = None
    # data type: WebsocketCommentsList
    oneof_schema_2_validator: Optional[WebsocketCommentsList] = None
    # data type: WebsocketTestMessage
    oneof_schema_3_validator: Optional[WebsocketTestMessage] = None
    actual_instance: Optional[Union[WebsocketCommentCreated, WebsocketCommentsList, WebsocketTestMessage]] = None
    one_of_schemas: List[str] = Literal["WebsocketCommentCreated", "WebsocketCommentsList", "WebsocketTestMessage"]

    model_config = {
        "validate_assignment": True,
        "protected_namespaces": (),
    }


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = WebsocketMessageData.model_construct()
        error_messages = []
        match = 0
        # validate data type: WebsocketCommentCreated
        if not isinstance(v, WebsocketCommentCreated):
            error_messages.append(f"Error! Input type `{type(v)}` is not `WebsocketCommentCreated`")
        else:
            match += 1
        # validate data type: WebsocketCommentsList
        if not isinstance(v, WebsocketCommentsList):
            error_messages.append(f"Error! Input type `{type(v)}` is not `WebsocketCommentsList`")
        else:
            match += 1
        # validate data type: WebsocketTestMessage
        if not isinstance(v, WebsocketTestMessage):
            error_messages.append(f"Error! Input type `{type(v)}` is not `WebsocketTestMessage`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in WebsocketMessageData with oneOf schemas: WebsocketCommentCreated, WebsocketCommentsList, WebsocketTestMessage. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in WebsocketMessageData with oneOf schemas: WebsocketCommentCreated, WebsocketCommentsList, WebsocketTestMessage. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: dict) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into WebsocketCommentCreated
        try:
            instance.actual_instance = WebsocketCommentCreated.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into WebsocketCommentsList
        try:
            instance.actual_instance = WebsocketCommentsList.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into WebsocketTestMessage
        try:
            instance.actual_instance = WebsocketTestMessage.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into WebsocketMessageData with oneOf schemas: WebsocketCommentCreated, WebsocketCommentsList, WebsocketTestMessage. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into WebsocketMessageData with oneOf schemas: WebsocketCommentCreated, WebsocketCommentsList, WebsocketTestMessage. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        to_json = getattr(self.actual_instance, "to_json", None)
        if callable(to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Dict:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        to_dict = getattr(self.actual_instance, "to_dict", None)
        if callable(to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


